{
  "name": "swipertemplate",
  "version": "1.0.0",
  "description": "## 简介",
  "main": "index.js",
  "scripts": {
    "dev": "npx browser-sync start --files \"*.html, css/*.css, js/*.js\" --server",
    "make": "rm -f index.aspx && cp template.aspx index.aspx && cat index.html >> index.aspx",
    "clean": "rm -f index.aspx"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.26.5"
  }
}
